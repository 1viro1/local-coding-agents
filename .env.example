# Copy to .env and fill in locally. Do not commit .env.
# OpenClaw + Ollama local-only quick path (see docs/setup-openclaw.md).
OLLAMA_API_KEY=ollama-local

# Optional: gateway token override when running `openclaw gateway --token`
# OPENCLAW_GATEWAY_TOKEN=replace-with-openssl-rand-hex-32
